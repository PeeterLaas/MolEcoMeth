# --- Quarto ---
/.quarto/
/_site/
**/*.quarto_ipynb

# --- Rendered decks kept out of the repository; CI builds them ---
/lectures/*.html
/lectures/*_files/
/*.html

# --- Source PDFs the lectures were built from (large, and not ours to share) --
*.pdf
!seminars/slides/*.pdf

# --- Figure libraries extracted from a textbook: staging only ---
# Same reasoning as the PDFs above — not ours to redistribute, and 48 MB of
# it. The dozen figures a deck actually uses are copied into assets/<deck>/
# and published from there under institutional teaching permissions.
/figures_*/

# --- Bulk data: download it, do not commit it ---
bw_assessment_eea_datahub_1990_2024_data.csv

# --- R / RStudio ---
.Rproj.user/
.Rhistory
.RData
.Ruserdata
rsconnect/

# --- OS ---
.DS_Store
Thumbs.db

# --- Leftovers from an unrelated Shiny app in this folder ---
/www/
/MolEcoMeth/
/app.R
/deploy.R
/.rscignore

# --- Decorative photos no longer used by any deck (11 MB); kept on disk ---
/assets/photos/dec_pic_*.jpg


# --- Python byte-cache from running the tools/ scripts ---
__pycache__/
